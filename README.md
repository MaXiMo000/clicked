# clicked

> **Deprecated: `clicked-evidence` is now part of [`receipt-evidence`](https://github.com/MaXiMo000/receipt).**
>
> ```bash
> pip install receipt-evidence
> ```
>
> `from receipt_evidence.browser import capture` replaces `from clicked import capture`. This package's final release (0.2.0) just installs
> `receipt-evidence` and keeps the old `import clicked` paths working with a
> deprecation warning. Everything that was here -- browser-interaction capture, its tests and its
> docs -- lives in the [receipt](https://github.com/MaXiMo000/receipt) repo now.

MIT licensed.
